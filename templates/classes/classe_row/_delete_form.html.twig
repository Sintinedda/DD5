<form method="post" action="{{ path('app_classes_classe_row_delete', {'id': classe_row.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ classe_row.id) }}">
    <button class="btn">Delete</button>
</form>
