<form method="post" action="{{ path('classe_row_delete', {'id': classe.id, 'id': r.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ r.id) }}">
    <button class="btn">Delete</button>
</form>
