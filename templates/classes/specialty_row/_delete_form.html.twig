<form method="post" action="{{ path('specialty_row_delete', {'id': id, 'id2': id2, 'id3': r.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ r.id) }}">
    <button class="btn">Delete</button>
</form>
