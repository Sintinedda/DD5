<form method="post" action="{{ path('specialty_skill_delete', {'id': id, 'id2': id2, 'id3': s.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ s.id) }}">
    <button class="btn">Delete</button>
</form>
