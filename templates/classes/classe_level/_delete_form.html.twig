<form method="post" action="{{ path('app_classes_classe_level_delete', {'id': classe_level.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ classe_level.id) }}">
    <button class="btn">Delete</button>
</form>
