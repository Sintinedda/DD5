<form method="post" action="{{ path('app_classes_specialty_liste_delete', {'id': specialty_liste.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ specialty_liste.id) }}">
    <button class="btn">Delete</button>
</form>
