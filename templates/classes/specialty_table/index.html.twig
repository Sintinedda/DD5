{% extends 'base.html.twig' %}

{% block title %}SpecialtyTable index{% endblock %}

{% block body %}
    <h1>SpecialtyTable index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Number</th>
                <th>Place</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for specialty_table in specialty_tables %}
            <tr>
                <td>{{ specialty_table.id }}</td>
                <td>{{ specialty_table.number }}</td>
                <td>{{ specialty_table.place }}</td>
                <td>{{ specialty_table.name }}</td>
                <td>
                    <a href="{{ path('app_classes_specialty_table_show', {'id': specialty_table.id}) }}">show</a>
                    <a href="{{ path('app_classes_specialty_table_edit', {'id': specialty_table.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_classes_specialty_table_new') }}">Create new</a>
{% endblock %}
