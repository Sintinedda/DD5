{% extends 'base.html.twig' %}

{% block title %}SpecialtyTable{% endblock %}

{% block body %}
    <h1>SpecialtyTable</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ specialty_table.id }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ specialty_table.number }}</td>
            </tr>
            <tr>
                <th>Place</th>
                <td>{{ specialty_table.place }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ specialty_table.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_classes_specialty_table_index') }}">back to list</a>

    <a href="{{ path('app_classes_specialty_table_edit', {'id': specialty_table.id}) }}">edit</a>

    {{ include('classes/specialty_table/_delete_form.html.twig') }}
{% endblock %}
