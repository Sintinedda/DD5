<form method="post" action="{{ path('bgrow_delete', {'id': b_g_row.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ b_g_row.id) }}">
    <button class="btn">Delete</button>
</form>
