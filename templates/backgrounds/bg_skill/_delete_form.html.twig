<form method="post" action="{{ path('bgskill_delete', {'id': b_g_skill.id, 'id2': bg.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ b_g_skill.id) }}">
    <button class="btn">Delete</button>
</form>
