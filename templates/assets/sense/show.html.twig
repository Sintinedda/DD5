{% extends 'base.html.twig' %}

{% block title %}Sense{% endblock %}

{% block body %}
    <h1>Sense</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sense.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ sense.name }}</td>
            </tr>
            <tr>
                <th>Abbreviation</th>
                <td>{{ sense.abbreviation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_assets_sense_index') }}">back to list</a>

    <a href="{{ path('app_assets_sense_edit', {'id': sense.id}) }}">edit</a>

    {{ include('assets/sense/_delete_form.html.twig') }}
{% endblock %}
