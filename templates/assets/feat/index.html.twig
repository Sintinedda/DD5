{% extends 'base.html.twig' %}

{% block title %}Feat index{% endblock %}

{% block body %}
    <h1>Feat index</h1>
    <a href="{{ path('admin') }}">Retour à l'accueil<a/>
    <br/><br/>
    <a href="{{ path('feat_new') }}">Create new</a>
    <br/><br/>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Slug</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
            {% for feat in feats %}
                <tr>
                    <td>{{ feat.id }}</td>
                    <td><a href="{{ path('feat_edit', {'id': feat.id}) }}">{{ feat.name }}</a></td>
                    <td>{{ feat.slug }}</td>
                    <td>{{ include('assets/feat/_delete_form.html.twig') }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

{% endblock %}
