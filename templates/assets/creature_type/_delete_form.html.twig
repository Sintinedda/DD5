<form method="post" action="{{ path('app_assets_creature_type_delete', {'id': creature_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ creature_type.id) }}">
    <button class="btn">Delete</button>
</form>
