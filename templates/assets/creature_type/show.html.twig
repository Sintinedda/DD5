{% extends 'base.html.twig' %}

{% block title %}CreatureType{% endblock %}

{% block body %}
    <h1>CreatureType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ creature_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ creature_type.name }}</td>
            </tr>
            <tr>
                <th>Abbreviation</th>
                <td>{{ creature_type.abbreviation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_assets_creature_type_index') }}">back to list</a>

    <a href="{{ path('app_assets_creature_type_edit', {'id': creature_type.id}) }}">edit</a>

    {{ include('assets/creature_type/_delete_form.html.twig') }}
{% endblock %}
