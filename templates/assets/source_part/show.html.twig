{% extends 'base.html.twig' %}

{% block title %}SourcePart{% endblock %}

{% block body %}
    <h1>SourcePart</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ source_part.id }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ source_part.number }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ source_part.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_assets_source_part_index') }}">back to list</a>

    <a href="{{ path('app_assets_source_part_edit', {'id': source_part.id}) }}">edit</a>

    {{ include('assets/source_part/_delete_form.html.twig') }}
{% endblock %}
