{% extends 'base.html.twig' %}

{% block title %}Condition{% endblock %}

{% block body %}
    <h1>Condition</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ condition.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ condition.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_assets_condition_index') }}">back to list</a>

    <a href="{{ path('app_assets_condition_edit', {'id': condition.id}) }}">edit</a>

    {{ include('assets/condition/_delete_form.html.twig') }}
{% endblock %}
