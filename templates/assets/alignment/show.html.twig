{% extends 'base.html.twig' %}

{% block title %}Alignment{% endblock %}

{% block body %}
    <h1>Alignment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ alignment.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ alignment.name }}</td>
            </tr>
            <tr>
                <th>Abbreviation</th>
                <td>{{ alignment.abbreviation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_assets_alignment_index') }}">back to list</a>

    <a href="{{ path('app_assets_alignment_edit', {'id': alignment.id}) }}">edit</a>

    {{ include('assets/alignment/_delete_form.html.twig') }}
{% endblock %}
