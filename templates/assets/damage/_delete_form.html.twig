<form method="post" action="{{ path('app_assets_damage_delete', {'id': damage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ damage.id) }}">
    <button class="btn">Delete</button>
</form>
