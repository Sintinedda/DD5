{% extends 'base.html.twig' %}

{% block title %}Damage{% endblock %}

{% block body %}
    <h1>Damage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ damage.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ damage.name }}</td>
            </tr>
            <tr>
                <th>Abbreviation</th>
                <td>{{ damage.abbreviation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_assets_damage_index') }}">back to list</a>

    <a href="{{ path('app_assets_damage_edit', {'id': damage.id}) }}">edit</a>

    {{ include('assets/damage/_delete_form.html.twig') }}
{% endblock %}
