<form method="post" action="{{ path('app_construct_table_row_delete', {'id': table_row.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ table_row.id) }}">
    <button class="btn">Delete</button>
</form>
