<form method="post" action="{{ path('item_skill_cat_delete', {'id': i.id, 'id2': item_category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ i.id) }}">
    <button class="btn">Delete</button>
</form>
