{% extends 'base.html.twig' %}

{% block title %}ItemProperty{% endblock %}

{% block body %}
    <h1>ItemProperty</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ item_property.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ item_property.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ item_property.slug }}</td>
            </tr>
            <tr>
                <th>Part</th>
                <td>{{ item_property.part }}</td>
            </tr>
            <tr>
                <th>D</th>
                <td>{{ item_property.d }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_items_item_property_index') }}">back to list</a>

    <a href="{{ path('app_items_item_property_edit', {'id': item_property.id}) }}">edit</a>

    {{ include('items/item_property/_delete_form.html.twig') }}
{% endblock %}
