<form method="post" action="{{ path('item_sub_delete', {'id': s.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ s.id) }}">
    <button class="btn">Delete</button>
</form>
