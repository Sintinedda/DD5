{{ form_start(form) }}
    <div>
        {{ form_row(form.name) }}
        {{ form_row(form.slug) }}
        {{ form_row(form.place) }}
    </div>
    <div>
        {{ form_row(form.d1) }}
    </div>
    <div>
        {{ form_row(form.d2) }}
    </div>
    <div>
        {{ form_row(form.d3) }}
    </div>
    <div>
        {{ form_row(form.d4) }}
    </div>
    <div>
        {{ form_row(form.d5) }}
    </div>
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
