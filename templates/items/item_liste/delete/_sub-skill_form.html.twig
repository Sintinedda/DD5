<form method="post" action="{{ path('item_liste_skill_sub_delete', {'id': l.id, 'id2': item_subcategory.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ l.id) }}">
    <button class="btn">Delete</button>
</form>