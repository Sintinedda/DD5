<form method="post" action="{{ path('items_delete', {'id': item_category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ item_category.id) }}">
    <button class="btn">Delete</button>
</form>
