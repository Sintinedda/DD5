{% extends 'base.html.twig' %}

{% block title %}ItemTable{% endblock %}

{% block body %}
    <h1>ItemTable</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ item_table.id }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ item_table.number }}</td>
            </tr>
            <tr>
                <th>Place</th>
                <td>{{ item_table.place }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ item_table.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_items_item_table_index') }}">back to list</a>

    <a href="{{ path('app_items_item_table_edit', {'id': item_table.id}) }}">edit</a>

    {{ include('items/item_table/_delete_form.html.twig') }}
{% endblock %}
