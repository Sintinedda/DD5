<form method="post" action="{{ path('item_table_sub_delete', {'id': t.id, 'id2': item_subcategory.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ t.id) }}">
    <button class="btn">Delete</button>
</form>