{% extends 'base.html.twig' %}

{% block title %}ItemTable index{% endblock %}

{% block body %}
    <h1>ItemTable index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Number</th>
                <th>Place</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for item_table in item_tables %}
            <tr>
                <td>{{ item_table.id }}</td>
                <td>{{ item_table.number }}</td>
                <td>{{ item_table.place }}</td>
                <td>{{ item_table.name }}</td>
                <td>
                    <a href="{{ path('app_items_item_table_show', {'id': item_table.id}) }}">show</a>
                    <a href="{{ path('app_items_item_table_edit', {'id': item_table.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_items_item_table_new') }}">Create new</a>
{% endblock %}
