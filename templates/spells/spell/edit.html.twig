{% extends 'base.html.twig' %}

{% block title %}Edit Spell{% endblock %}

{% block body %}
    <h1>Edit Spell</h1>

    {{ include('spells/spell/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_spells_spell_index') }}">back to list</a>

    {{ include('spells/spell/_delete_form.html.twig') }}
{% endblock %}
