<form method="post" action="{{ path('app_spells_spell_liste_delete', {'id': spell_liste.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ spell_liste.id) }}">
    <button class="btn">Delete</button>
</form>
