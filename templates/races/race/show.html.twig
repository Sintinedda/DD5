{% extends 'base.html.twig' %}

{% block title %}Race{% endblock %}

{% block body %}
    <h1>Race</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ race.id }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ race.slug }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ race.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_races_race_index') }}">back to list</a>

    <a href="{{ path('app_races_race_edit', {'id': race.id}) }}">edit</a>

    {{ include('races/race/_delete_form.html.twig') }}
{% endblock %}
