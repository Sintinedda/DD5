{{ form_start(form) }}
    {{ form_errors(form) }}
    <div>
        {{ form_row(form.slug) }}
        {{ form_row(form.name) }}
        {{ form_row(form.type) }}
    </div>
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
