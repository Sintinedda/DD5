<form method="post" action="{{ path('app_races_race_table_delete', {'id': race_table.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ race_table.id) }}">
    <button class="btn">Delete</button>
</form>
