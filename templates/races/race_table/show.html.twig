{% extends 'base.html.twig' %}

{% block title %}RaceTable{% endblock %}

{% block body %}
    <h1>RaceTable</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ race_table.id }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ race_table.number }}</td>
            </tr>
            <tr>
                <th>Place</th>
                <td>{{ race_table.place }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ race_table.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_races_race_table_index') }}">back to list</a>

    <a href="{{ path('app_races_race_table_edit', {'id': race_table.id}) }}">edit</a>

    {{ include('races/race_table/_delete_form.html.twig') }}
{% endblock %}
