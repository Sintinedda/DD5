<form method="post" action="{{ path('ssrace_liste_delete', {'id': race_subcategory.id, 'id2': l.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ l.id) }}">
    <button class="btn">Delete</button>
</form>