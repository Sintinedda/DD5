<form method="post" action="{{ path('app_races_race_liste_delete', {'id': race_liste.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ race_liste.id) }}">
    <button class="btn">Delete</button>
</form>
