<form method="post" action="{{ path('srace_delete', {'id': race_source.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ race_source.id) }}">
    <button class="btn">Delete</button>
</form>
