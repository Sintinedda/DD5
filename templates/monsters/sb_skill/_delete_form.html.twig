<form method="post" action="{{ path('monster_skill_delete', {'id': sb.id, 'id2': s.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ s.id) }}">
    <button class="btn">Delete</button>
</form>
