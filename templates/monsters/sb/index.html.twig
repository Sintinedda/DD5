{% extends 'base.html.twig' %}

{% block title %}SB index{% endblock %}

{% block body %}
    <h1>SB index</h1>
    <a href="{{ path('monster_new') }}">Create new</a>
    <br/><br/>
    <ul>
        {% for sb in sbs %}
            <li><a href="{{ path('monster_show', {'id': sb.id}) }}">{{ sb.name }}</a></li>
        {% endfor %}
    </ul>
{% endblock %}
