<form method="post" action="{{ path('app_monsters_s_b_delete', {'id': s_b.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ s_b.id) }}">
    <button class="btn">Delete</button>
</form>
